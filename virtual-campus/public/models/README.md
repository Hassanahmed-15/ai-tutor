# Avatar asset

The character files are locally bundled from the official Three.js example asset collection so the campus does not depend on runtime model requests. Source URLs are recorded in `CREDITS.md` at the module root.
