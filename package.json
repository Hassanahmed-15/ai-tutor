{
  "name": "ai-tutor",
  "private": true,
  "workspaces": [
    "frontend/*",
    "backend/packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend/web",
    "build": "npm run build --workspace=frontend/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "dependencies": {
    "docx": "^9.7.1",
    "onnxruntime-web": "^1.30.0"
  }
}
